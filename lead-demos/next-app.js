const c=LEADS[new URLSearchParams(location.search).get('site')]||LEADS.cousins;document.documentElement.style.cssText=`--p:${c.p};--p2:${c.p2};--a:${c.a}`;document.title=c.name+' | '+c.cat;const cards=c.services.map((x,i)=>`<article class="card"><div class="icon">${c.icons[i]}</div><h3>${x[0]}</h3><p>${x[1]}</p></article>`).join('');const steps=c.steps.map((x,i)=>`<article class="step"><div class="num">${i+1}</div><h3>${x[0]}</h3><p>${x[1]}</p></article>`).join('');const ticks=c.checks.map(x=>`<div>${x}</div>`).join('');const mapBtn=`<a class="btn" href="${c.map}" target="_blank" rel="noopener">Google Maps ↗</a>`;document.getElementById('app').innerHTML=`<div class="notice"><div class="wrap"><span class="hide">${c.cat} enquiries for local customers</span><a href="tel:${c.tel}">Call ${c.phone}</a></div></div><div class="nav-shell"><div class="wrap nav"><a class="brand" href="#home"><span class="logo">${c.main}</span><span>${c.short}<small>${c.cat}</small></span></a><button class="menu" aria-label="Open menu">☰</button><nav class="links"><a href="#services">Services</a><a href="#about">About</a><a href="#contact">Contact</a><a class="btn primary" href="tel:${c.tel}">Call now</a></nav></div></div><main><section class="hero" id="home"><div class="wrap grid"><div><span class="eyebrow"><span class="dot"></span>${c.tag}</span><h1>${c.title}</h1><p class="lead">${c.desc}</p><div class="actions"><a class="btn primary" href="tel:${c.tel}">☎ Call ${c.phone}</a>${mapBtn}</div><div class="proof"><span>★ <b>${c.rating}</b> Google rating</span><span>● <b>${c.reviews}</b> reviews</span></div>${c.cat==='Gas engineer'?'<p class="safety">For gas work, customers should confirm current Gas Safe registration and the exact scope directly with the engineer before booking.</p>':''}</div><div class="art"><div class="emoji">${c.main}</div><div class="local"><small>Local listing</small>${c.area}</div></div></div></section><section class="section" id="services"><div class="wrap"><div class="head"><span class="k">How the business can help</span><h2>Clear services, direct contact</h2><p>Business-specific content designed to turn local searches into genuine enquiries.</p></div><div class="cards">${cards}</div></div></section><section class="section dark"><div class="wrap"><div class="head"><span class="k">Simple customer journey</span><h2>From first call to a clear next step</h2><p>A practical, easy-to-understand route from enquiry to service or collection.</p></div><div class="steps">${steps}</div></div></section><section class="section" id="about"><div class="wrap grid about"><div><span class="k">Local business profile</span><h2>${c.about}</h2><p class="lead">${c.aboutp}</p><div class="ticks">${ticks}</div></div><aside class="details"><span class="k">Business details</span><h3>${c.name}</h3><div class="row">📍 ${c.place}</div><div class="row">☎ <a href="tel:${c.tel}">${c.phone}</a></div><div class="row">★ ${c.rating} from ${c.reviews} Google reviews</div></aside></div></section><section class="section contact" id="contact"><div class="wrap contact-box"><div class="grid"><div><span class="k" style="color:#fff">Get in touch</span><h2>${c.cta}</h2><p>${c.ctap}</p></div><div class="contact-buttons"><a class="btn primary" href="tel:${c.tel}">Call ${c.phone}</a>${mapBtn}</div></div></div></section></main><footer><div class="wrap footer"><span>© <span id="year"></span> ${c.name}. All rights reserved.</span><span class="concept">Concept website prepared for business review.</span></div></footer>`;document.querySelector('.menu').onclick=()=>document.querySelector('.links').classList.toggle('open');document.querySelectorAll('.links a').forEach(a=>a.onclick=()=>document.querySelector('.links').classList.remove('open'));document.getElementById('year').textContent=new Date().getFullYear();